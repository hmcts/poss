<?xml version="1.0" encoding="UTF-8"?>
<Question xmlns:html="http://www.w3.org/TR/REC-html40" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://eds.com/questionxhtml ..\Question.xsd">
	<Id>215</Id>
	<Label>Date by which party must inform court</Label>
	<ObjectName>PartyInformDate</ObjectName>
	<Html>	
		<script type="text/javascript">
			WP_DatePickerRenderer.createInline("PartyInformDate");
		</script>
	</Html>
	<Javascript>
		<XPath>/ds/EnterVariableData/FastTrack/partyInformDate</XPath>
		<HintText>Enter date by which party must inform court in the format DD-MON-YYYY</HintText>
		<mandatoryOn>PartyInformDate.dataBinding</mandatoryOn>
		<isMandatory>return true;</isMandatory>	
		<enableOn>IsInstructionNotified.dataBinding</enableOn>
		<isEnabled>return('Y' == Services.getValue(IsInstructionNotified.dataBinding));</isEnabled>	
		<logicOn>IsInstructionNotified.dataBinding</logicOn>
		<logic>
			if('Y' != Services.getValue(IsInstructionNotified.dataBinding))
			{	Services.setValue(PartyInformDate.dataBinding, "");
			}
		</logic>
		<validateOn>PartyInformDate.dataBinding</validateOn>
		<validate>
			//cannot be in past : working day only
			
			var checkDate = Services.getValue(PartyInformDate.dataBinding); 
			var err = null;
			
			if( false == isFutureDate(checkDate))
			{ 	err = ErrorCode.getErrorCode("CaseMan_dateCannotBeInThePast_Msg");
			}
			else if( isNonWorkingDate(checkDate))
		    {   err = ErrorCode.getErrorCode("CaseMan_dateNonWorkingDay_Msg"); 
		    }
			
			return err;
		</validate>	
		<!--initialise>		  	
		  	loadNonWorkingDays();
		</initialise-->
		<miscCode>
		    PartyInformDate.weekends = false;
		</miscCode>	
	</Javascript>
</Question>