<?xml version="1.0" encoding="UTF-8"?>
<Question xmlns:html="http://www.w3.org/TR/REC-html40" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://eds.com/questionxhtml ..\Question.xsd">
	<Id>81</Id>
	<Label>Date of agreement</Label>
	<ObjectName>HirePurchaseAgreementDate</ObjectName>
	<Html>	  
	  <script type="text/javascript">WP_DatePickerRenderer.createInline("HirePurchaseAgreementDate", false);</script>
	</Html>
	<Javascript>
		<XPath>/ds/EnterVariableData/Order/hpdate</XPath>
		<mandatoryOn>HirePurchaseAgreementDate.dataBinding</mandatoryOn>
		<isMandatory>return true;</isMandatory>	
		<HintText>Select or enter the date of the agreement</HintText>		
		<validateOn>HirePurchaseAgreementDate.dataBinding</validateOn>
		<validate>
			//date cannot be in the future
			var isFuture = isFutureDate(Services.getValue(HirePurchaseAgreementDate.dataBinding));
			var err = null;
			if( true == isFuture)
			{   err = ErrorCode.getErrorCode("CaseMan_dateCannotBeInTheFuture_Msg");				
			}
			return err;
		</validate>
		
		<Context ScreenId="O_7_8">
            <isEnabled>return true;</isEnabled> 
			<validate>
				//Date picker: error message “The value does not conform to a valid date format of DD-MMM-YYYY”.
				//Validation: Date cannot be in the future
                return isInPast(Services.getValue(HirePurchaseAgreementDate.dataBinding),"false");
			</validate>
		</Context>
		
	</Javascript>
</Question>