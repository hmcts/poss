<?xml version="1.0" encoding="UTF-8"?>
<Question xmlns:html="http://www.w3.org/TR/REC-html40" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://eds.com/questionxhtml ..\Question.xsd">
	<Id>230</Id>
	<Label>Date for exchange</Label>
	<ObjectName>ExchangeDate</ObjectName>
	<Html>	
		<script type="text/javascript">
			WP_DatePickerRenderer.createInline("ExchangeDate");
		</script>
	</Html>
	<Javascript>
		<XPath>/ds/EnterVariableData/FastTrack/exchangeDate</XPath>
		<HintText>Enter date for exchange in the format DD-MON-YYYY</HintText>
		<mandatoryOn>ExchangeDate.dataBinding</mandatoryOn>
		<isMandatory>return true;</isMandatory>	
		<enableOn>SimOrSeqExch.dataBinding</enableOn>
		<isEnabled>return('SIM' == Services.getValue(SimOrSeqExch.dataBinding));</isEnabled>
		<logicOn>SimOrSeqExch.dataBinding</logicOn>
		<logic>
			if('SIM' != Services.getValue(SimOrSeqExch.dataBinding))
			{	Services.setValue(ExchangeDate.dataBinding, "");
			}
		</logic>
		<validateOn>ExchangeDate.dataBinding</validateOn>
		<validate>
			//cannot be in past : working day only
			
			var checkDate = Services.getValue(ExchangeDate.dataBinding); 
			var err = null;
			
			if( false == isFutureDate(checkDate))
			{ 	err = ErrorCode.getErrorCode("CaseMan_dateCannotBeInThePast_Msg");
			}
			else if( isNonWorkingDate(checkDate))
		    {   err = ErrorCode.getErrorCode("CaseMan_dateNonWorkingDay_Msg"); 
		    }
			
			return err;
		</validate>	
		<!--initialise>		  	
		  	loadNonWorkingDays();
		</initialise-->
		<miscCode>
		    ExchangeDate.weekends = false;
		</miscCode>	
	</Javascript>
</Question>