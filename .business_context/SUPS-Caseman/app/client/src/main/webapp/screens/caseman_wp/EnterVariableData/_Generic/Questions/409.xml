<?xml version="1.0" encoding="UTF-8"?>
<Question xmlns:html="http://www.w3.org/TR/REC-html40" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://eds.com/questionxhtml ..\Question.xsd">
	<Id>409</Id>
	<Label>Date by which documents should be filed</Label>
	<ObjectName>DocumentsFiledByDate2</ObjectName>
	<Html>
		<script type="text/javascript">
			WP_DatePickerRenderer.createInline("DocumentsFiledByDate2");
		</script>
	</Html>
	
	<Javascript>
		<XPath>/ds/EnterVariableData/SmallTrack/documentsFiledByDate2</XPath>
		<HintText>Enter the date by which documents to be filed in format DD-MON-YYYY</HintText>
		<isMandatory>return true;</isMandatory>
		<enableOn>IsPartyToClarify.dataBinding</enableOn>
		<isEnabled>return('Y' == Services.getValue(IsPartyToClarify.dataBinding));</isEnabled>
		<logicOn>IsPartyToClarify.dataBinding</logicOn>
		<logic>
			if('Y' != Services.getValue(IsPartyToClarify.dataBinding))
			{	Services.setValue(DocumentsFiledByDate2.dataBinding, "");
			}
		</logic>
		<validateOn>DocumentsFiledByDate2.dataBinding</validateOn>
		<validate>
			//cannot be in past : working day only
			
			var checkDate = Services.getValue(DocumentsFiledByDate2.dataBinding); 
			var err = null;
			
			if( false == isFutureDate(checkDate))
			{ 	err = ErrorCode.getErrorCode("CaseMan_dateCannotBeInThePast_Msg");
			}
			else if( isNonWorkingDate(checkDate))
		    {   err = ErrorCode.getErrorCode("CaseMan_dateNonWorkingDay_Msg"); 
		    }
			
			return err;
		</validate>	
		<!--initialise>		  	
		  	loadNonWorkingDays();
		</initialise-->
		<miscCode>
		    DocumentsFiledByDate2.weekends = false;
		</miscCode>	
	</Javascript>
</Question>