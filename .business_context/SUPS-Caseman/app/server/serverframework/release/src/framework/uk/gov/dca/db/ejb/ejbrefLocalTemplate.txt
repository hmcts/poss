	
	<ejb-local-ref>
		<ejb-ref-name>ejb/${service.name}ServiceLocal</ejb-ref-name>
		<ejb-ref-type>Session</ejb-ref-type>
		<local-home>${service.package}.${service.name}ServiceLocalHome</local-home>
		<local>${service.package}.${service.name}Service</local>
		<ejb-link>${service.name}Service</ejb-link>
	</ejb-local-ref>
	